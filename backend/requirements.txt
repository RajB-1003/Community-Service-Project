# Core (required)
fastapi
uvicorn[standard]
python-multipart
python-dotenv
pydantic

# Streamlit UI (app.py)
streamlit
requests

# Optional: only needed for POST /api/process (audio → Whisper transcription)
# Remove if running fully offline / text-only mode
groq
